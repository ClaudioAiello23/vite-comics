import { createApp } from 'vue'
import './assets/styles/main.scss'
import "@fontsource/montserrat"; // aggiunto Google fonts dopo installazione da npm
import App from './App.vue'


createApp(App).mount('#app')
